apiVersion: v1
kind: Service
metadata:
  name: {{name}}
  namespace: {{namespace}}
  labels:
    app: {{name}}
spec:
  type: NodePort
  selector:
    app: {{name}}
  ports:
    - name: http
      port: {{port}}
      targetPort: {{port}}
      protocol: TCP
