apiVersion: apps/v1
kind: Deployment
metadata:
  name: promotion-svc
  namespace: product
  labels:
    app: promotion-svc
    model: BusinessModel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: promotion-svc
  template:
    metadata:
      labels:
        app: promotion-svc
        risk: medium
    spec:
      containers:
        - name: promotion-svc
          image: your-registry/promotion-svc:latest
          ports:
            - containerPort: 3009
          envFrom:
            - configMapRef:
                name: env
          env:
            - name: BUSINESS_MODEL
              value: "subscription"
            - name: SUBSCRIPTION_PRICE
              value: "199000"
            - name: SUBSCRIPTION_FREQUENCY
              value: "monthly"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3009
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3009
            initialDelaySeconds: 10
            periodSeconds: 5
      restartPolicy: Always
