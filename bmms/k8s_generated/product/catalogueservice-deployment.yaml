apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalogue-svc
  namespace: product
  labels:
    app: catalogue-svc
    model: MultiBusinessModel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalogue-svc
  template:
    metadata:
      labels:
        app: catalogue-svc
        risk: medium
    spec:
      containers:
        - name: catalogue-svc
          image: your-registry/catalogue-svc:latest
          ports:
            - containerPort: 3007
          envFrom:
            - configMapRef:
                name: env
          env:
            - name: BUSINESS_MODEL
              value: "multi"
            - name: SUPPORTED_MODELS
              value: "retail,subscription"
            - name: RETAIL_PRODUCTS_COUNT
              value: "2"
            - name: SUBSCRIPTION_PLANS_COUNT
              value: "1"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 10
            periodSeconds: 5
      restartPolicy: Always
