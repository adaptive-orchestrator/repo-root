apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-svc
  namespace: finance
  labels:
    app: payment-svc
    model: BusinessModel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-svc
  template:
    metadata:
      labels:
        app: payment-svc
        risk: medium
    spec:
      containers:
        - name: payment-svc
          image: your-registry/payment-svc:latest
          ports:
            - containerPort: 3015
          envFrom:
            - configMapRef:
                name: env
          env:
            - name: BUSINESS_MODEL
              value: "subscription"
            - name: SUBSCRIPTION_PRICE
              value: "199000"
            - name: SUBSCRIPTION_FREQUENCY
              value: "monthly"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3015
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3015
            initialDelaySeconds: 10
            periodSeconds: 5
      restartPolicy: Always
