apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-svc
  namespace: customer
  labels:
    app: customer-svc
    model: MultiBusinessModel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customer-svc
  template:
    metadata:
      labels:
        app: customer-svc
        risk: medium
    spec:
      containers:
        - name: customer-svc
          image: your-registry/customer-svc:latest
          ports:
            - containerPort: 3001
          envFrom:
            - configMapRef:
                name: env
          env:
            - name: BUSINESS_MODEL
              value: "multi"
            - name: SUPPORTED_MODELS
              value: "retail,subscription"
            - name: RETAIL_PRODUCTS_COUNT
              value: "2"
            - name: SUBSCRIPTION_PLANS_COUNT
              value: "1"
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1024Mi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 5
      restartPolicy: Always
