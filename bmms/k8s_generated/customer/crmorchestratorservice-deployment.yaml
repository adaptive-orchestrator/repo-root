apiVersion: apps/v1
kind: Deployment
metadata:
  name: crm-orchestrator
  namespace: customer
  labels:
    app: crm-orchestrator
    model: MultiBusinessModel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crm-orchestrator
  template:
    metadata:
      labels:
        app: crm-orchestrator
        risk: medium
    spec:
      containers:
        - name: crm-orchestrator
          image: your-registry/crm-orchestrator:latest
          ports:
            - containerPort: 3002
          envFrom:
            - configMapRef:
                name: env
          env:
            - name: BUSINESS_MODEL
              value: "multi"
            - name: SUPPORTED_MODELS
              value: "retail,subscription"
            - name: RETAIL_PRODUCTS_COUNT
              value: "2"
            - name: SUBSCRIPTION_PLANS_COUNT
              value: "1"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 5
      restartPolicy: Always
