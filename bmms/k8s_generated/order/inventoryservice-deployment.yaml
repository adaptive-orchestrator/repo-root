apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-svc
  namespace: order
  labels:
    app: inventory-svc
    model: MultiBusinessModel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-svc
  template:
    metadata:
      labels:
        app: inventory-svc
        risk: medium
    spec:
      containers:
        - name: inventory-svc
          image: your-registry/inventory-svc:latest
          ports:
            - containerPort: 3013
          envFrom:
            - configMapRef:
                name: env
          env:
            - name: BUSINESS_MODEL
              value: "multi"
            - name: SUPPORTED_MODELS
              value: "retail,subscription"
            - name: RETAIL_PRODUCTS_COUNT
              value: "2"
            - name: SUBSCRIPTION_PLANS_COUNT
              value: "1"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3013
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3013
            initialDelaySeconds: 10
            periodSeconds: 5
      restartPolicy: Always
